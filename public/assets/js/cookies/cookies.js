/*
 * 
 * Este arquivo está sendo carregado em ( login/indexView, inicio/inicioView )
 * Neste arquivo contém apenas a rota para o painel
 * Este cookie está sendo setado no UtilizadorDAO metodo DadosUtilizador()
 * A desactivação do cookie está em painel/terminarSessaoView.php
 * 
 */

if (localStorage.getItem("cookies") != null ) {
    //    window.location = "/painel";
}

